declare module '../assets' {
  const tagType: string;
  const createCampaign: string;
  const dashboard: string;
  const logo: string;
  const logout: string;
  const payment: string;
  const profile: string;
  const sun: string;
  const withdraw: string;
  const search: string;
  const menu: string;
  const money: string;
  const loader: string;
  const thirdweb: string;

  export {
    tagType,
    createCampaign,
    dashboard,
    logo,
    logout,
    payment,
    profile,
    sun,
    withdraw,
    search,
    menu,
    money,
    loader,
    thirdweb,
  };
}
